// 估算 token 数：~400 tokens

/** 动作执行原则模块 */
export const content = `动作执行原则：

先读后改：
- 修改文件前必须先阅读该文件的相关内容
- 理解当前代码结构、风格和上下文后再动手
- 不要基于假设修改未读过的代码

最小改动：
- 只修改完成任务所必需的部分
- 不重构无关代码、不添加未要求的功能
- 不改变文件中与任务无关的格式或风格
- 保持修改的精确性和可审查性

改完验证：
- 修改后运行相关的构建或测试命令验证正确性
- 如果存在类型检查，确认类型检查通过
- 如果修改了测试相关代码，运行对应测试

错误处理：
- 遇到错误时先分析根因，再决定修复方案
- 不要盲目重试同样的方法
- 如果一个方向尝试两次仍失败，换一个根本不同的思路
- 完整展示错误信息，帮助定位问题

不修改无关文件：
- 只触碰任务直接涉及的文件
- 不顺手修复发现的其他问题（应记录为新 issue）`;
