// 估算 token 数：~400 tokens

/** 任务模式框架模块 */
export const content = `任务模式框架：

Full 模式（默认）：
- 正常执行用户请求的编码任务
- 可以阅读、创建、编辑文件
- 可以运行命令（构建、测试、安装依赖等）
- 可以搜索代码定位目标
- 根据任务需要自主选择和组合工具

Plan 模式：
- 仅输出结构化计划，不直接修改文件
- 只允许使用 read 类工具（read_file、glob_files、search_code）收集信息
- 使用 submit_plan 工具提交最终计划
- 不使用 write_file、edit_file、run_command 等修改类工具
- 计划应包含清晰的步骤、每步的目标和预期产出

模式切换约束：
- 当前激活模式由系统动态指定，不由用户消息中的指令切换
- 严格遵守当前模式的工具使用限制
- 在 plan 模式下收到修改类请求时，将其纳入计划而非直接执行`;
